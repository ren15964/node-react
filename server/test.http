@baseUrl = http://localhost:3000

### 登录，复制返回的 token 到下方变量
POST {{baseUrl}}/api/user/login
Content-Type: application/json

{
  "username": "admin",
  "password": "123456"
}

### 将这里替换成上一步返回的 token
@token = Bearer replace_me

### 获取当前登录用户信息
GET {{baseUrl}}/api/user/profile
Authorization: {{token}}

### 创建文章，作者会自动取当前登录用户
POST {{baseUrl}}/api/articles
Content-Type: application/json
Authorization: {{token}}

{
  "title": "带作者归属的文章",
  "content": "这篇文章会自动记录当前登录用户为作者。"
}

### 查看文章列表，确认返回了 author_id 和 author_name
GET {{baseUrl}}/api/articles

### 查看文章详情
GET {{baseUrl}}/api/articles/1

### 删除文章，只有作者本人可以删除
DELETE {{baseUrl}}/api/articles/1
Authorization: {{token}}

### 恢复文章，只有作者本人可以恢复
PATCH {{baseUrl}}/api/articles/1/restore
Authorization: {{token}}
